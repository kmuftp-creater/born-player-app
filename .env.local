import { GoogleGenerativeAI } from "@google/genai";

// 1. 使用 Vite 的正確語法來讀取我們在 Zeabur 設定的環境變數
const apiKey = import.meta.env.VITE_GOOGLE_API_KEY;

// 2. 在初始化時傳入這個金鑰
const genAI = new GoogleGenerativeAI(apiKey);

// ... 後續使用 genAI 的程式碼
